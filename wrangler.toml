name = "adara-spa-transactions"
compatibility_date = "2024-01-01"
pages_build_output_dir = "public"

[vars]
# GHL_LOCATION_ID and GHL_API_KEY must be set as encrypted secrets in Cloudflare Pages dashboard
# Run: wrangler pages secret put GHL_API_KEY
# Run: wrangler pages secret put GHL_LOCATION_ID
